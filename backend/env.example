PORT=5000
MONGODB_URI=mongodb://localhost:27017/ifa-ems
JWT_SECRET=replace-with-a-secure-random-string
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
# Comma-separated list of URLs allowed to call the API (e.g. https://app.example.com,https://admin.example.com)
CORS_ORIGINS=http://localhost:5173
# FRONTEND_URL is used by Socket.IO if you only need a single origin.
FRONTEND_URL=http://localhost:5173
COOKIE_SAME_SITE=none
# Optional: share cookies across subdomains (e.g. .example.com)
COOKIE_DOMAIN=

